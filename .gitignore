__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
dist/
build/
.DS_Store
.claude/
# Audit logs written locally are ignored; the synthetic corpus (corpus/) is committed on purpose
# so the exact dataset is reproducible, not only regenerable from the generator.
audit.jsonl
*.audit.jsonl
